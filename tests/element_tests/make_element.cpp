#include "TACSMeshLoader.h"
#include "TACSAssembler.h"

// this example is based off of examples/crm/crm.cpp in TACS

int main() {
    // build the TACS mesh loader and scan the uCRM BDF file
    printf("Making shell element..\n");
    TACS
}